// Temporarily disabled SEO component due to react-helmet dependency
// This component will be re-enabled when react-helmet is properly configured

import React from 'react';

const SEO: React.FC = () => null;
export default SEO; 